---
type: Note
related_to:
  - "[[HeyGen]]"
  - "[[HyperFrames]]"
  - "[[Codex]]"
status: Active
---

# HyperFrames 与 Codex 剪视频工作流

## HyperFrames 是什么

[[HyperFrames]] 是 HeyGen 推出的 HTML 原生视频框架。核心思路是把视频从传统时间线素材，转成可以用 HTML/CSS/JS 描述和控制的结构。

可以把它理解成：

- 视频画面由组件组成
- 字幕、头像、图片、布局、动画可以代码化控制
- 更适合模板化、批量化、AI 驱动的视频生成
- 便于根据脚本、数据、用户属性自动生成多个版本

它更像“用前端方式做视频”，而不是传统 NLE 时间线剪辑器。

## 它适合什么场景

[[HyperFrames]] 更适合：

- AI 口播视频
- 产品介绍视频
- 营销短视频
- 多语言版本视频
- 批量个性化视频
- 自动字幕与动态排版

不太适合：

- 电影级复杂剪辑
- 重度调色
- 精细音频混音
- 强依赖手工镜头语言的项目

## 怎么结合 Codex

[[Codex]] 的作用不是直接拖动时间线，而是帮助生成、修改和维护视频代码。

一个实用工作流可以是：

```text
脚本/素材
  ↓
整理为 HyperFrames 结构
  ↓
让 Codex 生成或修改 HTML/CSS/JS 视频逻辑
  ↓
调用渲染流程输出 MP4
```

### 1. 先定义视频结构

可以先把视频拆成场景、字幕、头像、转场、动画等结构。

示意：

```json
{
  "scenes": [
    {
      "duration": 5,
      "title": "AI 自动生成视频",
      "subtitle": "今天介绍 HyperFrames",
      "avatar": "host",
      "layout": "centered"
    }
  ]
}
```

### 2. 让 Codex 改写剪辑逻辑

给 [[Codex]] 的典型指令：

- 把 60 秒视频压缩成 15 秒
- 只保留前三个核心观点
- 字幕改成大字居中
- 前 3 秒加标题卡
- 给每段口播补一个 B-roll 区块
- 输出 9:16 和 16:9 两个版本

对应地，[[Codex]] 可以生成类似逻辑：

```js
const shortScenes = scenes
  .filter(scene => scene.importance >= 0.8)
  .slice(0, 3)
  .map(scene => ({
    ...scene,
    duration: Math.min(scene.duration, 5),
    subtitleStyle: 'large-centered'
  }));
```

### 3. 用 AI 做内容理解，用 Codex 做工程执行

更完整的流程：

```text
原始脚本 / 素材
  ↓
转录、摘要、分段
  ↓
生成 HyperFrames 场景结构
  ↓
Codex 修改模板与样式
  ↓
渲染导出视频
```

其中 [[Codex]] 适合承担：

- 生成模板代码
- 调整场景顺序
- 修改字幕与样式
- 插入 CTA、封面、片尾
- 批量生成不同版本
- 接入渲染脚本输出成品

## 最佳实践

### 1. 先把视频当成“产品模板”，再当成“剪辑成片”

[[HyperFrames]] 更适合模板化视频生产，所以不要一开始就按传统时间线思路设计。更好的方式是先定义可复用结构：

- 开场 Hook
- 主体观点
- 视觉辅助区块
- 字幕样式
- CTA
- 片尾
- 横版、竖版、方版布局

这样后续 [[Codex]] 才能稳定地批量改版，而不是每次都从零生成。

### 2. 场景结构要语义化，不要只写时间点

不要只描述“第 0-5 秒显示文字”，而要描述这一段在视频里的作用。

推荐结构：

```json
{
  "sceneType": "hook",
  "goal": "吸引用户继续观看",
  "duration": 3,
  "message": "AI 视频可以像网页一样生成",
  "visualRole": "title-card",
  "priority": "high"
}
```

这样 [[Codex]] 在压缩、改写、重排视频时，能理解哪些内容必须保留，哪些可以删减。

### 3. 把内容、布局、样式、渲染参数分离

推荐拆成四层：

```text
内容层：脚本、字幕、场景文案
结构层：场景顺序、时长、转场、CTA
表现层：字体、颜色、动效、布局
输出层：尺寸、帧率、码率、语言版本
```

这样做的好处是：

- 改文案不影响视觉模板
- 改 9:16 / 16:9 不需要重写内容
- 多语言版本可以复用同一套结构
- [[Codex]] 修改时更不容易破坏整体工程

### 4. 给 Codex 明确“剪辑规则”，不要只给模糊目标

低质量指令：

> 帮我剪得更好看。

更好的指令：

> 把视频压缩到 30 秒以内；保留 hook、3 个核心卖点和 CTA；删除重复解释；每个场景不超过 5 秒；字幕使用大字居中；输出 9:16 竖版。

可复用规则示例：

```text
- 前 3 秒必须出现明确利益点
- 每 4-6 秒切换一次视觉状态
- 每个场景只表达一个观点
- 字幕单行不超过 14 个汉字
- CTA 必须出现在最后 5 秒
- 同一模板必须能输出 9:16、16:9、1:1
```

### 5. 建立素材与场景的映射表

如果视频里有头像、产品截图、B-roll、Logo、背景音乐，最好维护一份素材清单：

```json
{
  "assets": {
    "avatar_host": "主持人口播头像",
    "product_demo": "产品演示录屏",
    "logo": "品牌 Logo",
    "music_upbeat": "轻快背景音乐"
  }
}
```

然后在场景里引用素材 ID，而不是直接写死文件路径。这样 [[Codex]] 可以更安全地替换、复用和批量生成版本。

### 6. 先小样验证，再批量生成

推荐流程：

```text
单条脚本
  ↓
生成 1 个 HyperFrames 样片
  ↓
人工检查节奏、字幕、品牌样式
  ↓
固化模板与规则
  ↓
批量生成多语言、多比例、多版本
```

不要一开始就批量生成几十条视频。先验证模板质量，再扩大生成规模。

### 7. 保留人工审核节点

[[HyperFrames]] + [[Codex]] 适合自动化生产，但最终发布前仍建议人工审核：

- 字幕是否准确
- 品牌词是否正确
- 画面是否遮挡主体
- 音画是否同步
- CTA 是否合规
- 多语言版本是否有语义偏差

尤其是营销、金融、医疗、教育类内容，不建议完全无人审核后直接发布。

### 8. 推荐的工程目录

一个实用项目结构可以是：

```text
video-project/
  content/
    script.zh.md
    script.en.md
  data/
    scenes.json
    assets.json
  templates/
    short-video.html
    product-demo.html
  styles/
    brand.css
    subtitles.css
  render/
    render.js
    export-presets.json
  output/
    vertical.mp4
    horizontal.mp4
```

其中 [[Codex]] 主要负责修改 `data/`、`templates/`、`styles/` 和 `render/`，而不是直接操作最终 MP4。

### 9. 推荐的 Codex Prompt 模板

```text
你是一个视频模板工程助手。

目标：基于 HyperFrames 结构生成一个 30 秒以内的短视频版本。

输入：
- scenes.json：场景结构
- assets.json：素材清单
- brand.css：品牌样式

要求：
1. 保留 hook、3 个核心卖点、CTA
2. 删除重复解释和低优先级场景
3. 每个场景不超过 5 秒
4. 字幕大字居中，适合手机观看
5. 输出 9:16 和 16:9 两个布局
6. 不改变素材 ID，只修改引用和排版逻辑
7. 生成可渲染的 HTML/CSS/JS 代码
```

## 与传统剪辑软件的区别

传统剪辑更像：

```text
视频轨道 + 音频轨道 + 时间线
```

[[HyperFrames]] 更像：

```text
HTML 组件 + 样式系统 + 时间控制 + 渲染输出
```

所以它的优势不在“纯手工剪”，而在“程序化生成”和“AI 自动化批量生产”。

## 简单结论

一句话概括：

> [[HyperFrames]] 负责把视频表达成可编程结构，[[Codex]] 负责按你的剪辑目标生成和修改这套结构，最后再渲染成视频。

如果你后续要，我可以继续把这份文档扩成：

- 面向产品经理的说明版
- 面向工程实现的技术方案版
- 直接可执行的 prompt 模板版
- 一个示例项目结构


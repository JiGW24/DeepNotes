---
type: AI
related_to:
  - "[[Trellis]]"
_width: wide
---

# Trellis 调研与用法总结

## Trellis 是什么

Trellis 可以理解为面向 AI Coding 的 `agent harness + 项目知识层`。它的核心不是替代 IDE，而是在仓库里生成一套可版本化的规范、任务、工作流和会话记忆，让多个 AI 编码工具按同一套工程约定协作。

它解决的主要问题是：AI 在真实项目里容易缺上下文、缺流程、缺一致性。Trellis 通过仓库内文件把这些内容固定下来，而不是每次靠 prompt 临时补充。

## 核心组成

### 1. Spec
- 位置：`.trellis/spec/`
- 用途：沉淀项目规范、架构约定、测试策略、错误处理方式等
- 作用：让 AI 在不同会话、不同工具里都能读取同一套规则

### 2. Task
- 位置：`.trellis/tasks/`
- 用途：表示一个具体工作单元
- 常见内容：`prd.md`、`design.md`、`implement.md`、`research/`、上下文清单
- 作用：把需求、设计和实现边界文件化，避免 AI 直接跳进代码乱改

### 3. Workflow
- 位置：`.trellis/workflow.md`
- 用途：定义工作流阶段和技能路由
- 默认流程通常是：`Plan -> Execute -> Finish`

### 4. Skills / Sub-agents / Hooks
- 用途：把调研、实现、检查、收尾等动作模块化
- 典型能力：
  - 调研代码和文档
  - 按任务上下文实现功能
  - 基于 diff 做检查和自动修复
  - 在任务收尾时记录 journal 和归档

### 5. Journal / Workspace
- 位置：`.trellis/workspace/<developer>/`
- 用途：保存跨会话开发记忆
- 作用：让后续会话更稳定地恢复上下文，而不完全依赖聊天历史

## 初始化和接入

### 安装
```bash
npm install -g @mindfoldhq/trellis@beta
```

### 初始化
```bash
trellis init -u your-name
```

按平台初始化的常见写法：

```bash
trellis init -u your-name --claude
trellis init -u your-name --claude --cursor --opencode
trellis init -u your-name --codex --gemini
```

### 初始化后的典型结构
```text
.trellis/
  .developer
  .version
  workflow.md
  config.yaml
  spec/
  tasks/
  workspace/
  scripts/

.claude/
  commands/trellis/
  agents/
  skills/
  hooks/
```

不同平台会写入不同目录，但 `.trellis/` 是共享核心。

## 推荐使用流程

### 1. 直接描述任务
用户可以直接描述需求，例如“新增用户登录功能”。

Trellis 会根据任务复杂度判断：
- 简单问答：不必创建 task
- 小改动：可 inline 完成
- 复杂任务：建议进入完整 task 流程

### 2. Plan 阶段
目标是先明确任务，再开始写代码。

典型动作：
- 澄清需求
- 创建 task
- 产出 `prd.md`
- 必要时做代码/文档调研并写入 `research/`
- 复杂任务补充 `design.md`、`implement.md`
- 绑定本任务需要读取的 spec 和研究上下文

### 3. Execute 阶段
AI 在明确上下文后实施改动，主要依据：
- `prd.md`
- `design.md`
- `implement.md`
- `.trellis/spec/`
- `research/`

如果平台支持，Trellis 会调用专门的实现型 sub-agent。

### 4. Check 阶段
这是 Trellis 的关键价值之一。

检查阶段通常会：
- 查看 git diff
- 对照 spec review 改动
- 跑 lint、typecheck、test
- 能修复的问题自动修复
- 报告剩余风险和问题

相比只写规则文件，Trellis 的区别在于它把“检查”也纳入工作流，而不是只提醒 AI 注意规范。

### 5. Finish 阶段
收尾通常包括：
- 最终验证
- 归档任务
- 写 workspace journal
- 记录 session
- 必要时把新经验沉淀到 spec

常见命令：
```text
/trellis:finish-work
```

需要注意，`finish-work` 更偏 bookkeeping，不等于代码提交本身。代码提交和任务归档通常是两个动作。

## 常用命令

### Slash 命令
```text
/trellis:start
/trellis:continue
/trellis:finish-work
```

其中最常用的是：
- `/trellis:continue`：推进当前 task
- `/trellis:finish-work`：任务收尾与归档

### CLI 命令
```bash
trellis upgrade
trellis update
```

### task.py 常见操作
```bash
./.trellis/scripts/task.py create "新增用户登录" --slug user-login
./.trellis/scripts/task.py start "$TASK_DIR"
./.trellis/scripts/task.py finish
./.trellis/scripts/task.py list
./.trellis/scripts/task.py archive user-login
./.trellis/scripts/task.py validate "$TASK_DIR"
```

## 适合的场景

### 1. 新项目启动
适合在项目初期把目录结构、测试策略、API 风格、错误处理规范先沉淀成 spec。

### 2. 存量项目接入
适合把已有代码里的隐式规范提炼出来，形成团队共享的 `.trellis/spec/`。

### 3. 中大型功能交付
适合跨前后端、数据库、测试的任务，因为可以先固定 PRD 和设计，再约束执行。

### 4. 老模块重构
适合先调研现状，再拆成小步任务，降低 AI 大范围误改的风险。

### 5. 反复出现的 bug
适合通过额外分析和任务沉淀，逐步把补丁经验转化为更稳定的规范和检查项。

## 与类似 harness 工具的对比

### Trellis vs Claude Code 原生 `CLAUDE.md` / commands / agents
- 原生能力更轻，适合直接交互
- Trellis 在其上增加了 task、workflow、spec、journal 和检查闭环
- 如果团队已经在用 Claude Code，Trellis 的价值在于把零散配置组织成工程化流程

### Trellis vs Cursor Rules / `.cursorrules`
- Cursor Rules 更像规则注入
- Trellis 更像任务驱动的 AI 开发流程系统
- Trellis 的上下文结构化程度、跨会话能力和检查闭环更强

### Trellis vs Aider
- Aider 更偏“和 AI 一起快速改代码”
- Trellis 更偏“让 AI 在团队约束下稳定工作”
- 前者偏轻量协作，后者偏工程流程治理

### Trellis vs OpenHands / Devin 类自主 Agent
- 自主 Agent 更强调独立完成任务
- Trellis 更强调人可审查、流程可控、规范可沉淀
- 它不是全自动开发系统，而是现有 AI coding 工具的约束层

### Trellis vs LangGraph / AutoGen / CrewAI
- LangGraph / AutoGen / CrewAI 是构建 AI Agent 应用的框架
- Trellis 是约束软件开发场景下 AI 编码助手的流程框架
- 一个偏“做 agent 产品”，一个偏“管理 AI 参与写代码的方式”

### Trellis vs Continue.dev
- Continue.dev 更偏 IDE 助手和模型接入
- Trellis 更偏任务、规范、工作流和检查闭环

### Trellis vs GitHub Copilot instructions / Coding Agent
- Copilot instructions 偏 GitHub 生态内规则文件
- Trellis 更强调跨平台统一和任务化上下文

## Trellis 的优势

1. 多平台统一：不同 AI 编码工具可共享同一套项目规范
2. 规范资产化：spec 可版本化、可 review、可迭代
3. 任务过程可追踪：需求、设计、研究、实现边界都能落盘
4. 内置检查闭环：不只是写代码，还把验证纳入标准流程
5. 跨会话记忆：比单纯依赖聊天历史更稳定
6. 可定制：可以通过 `workflow.md` 和 `spec/` 按团队习惯调整

## Trellis 的局限

1. 会引入额外文件和流程，仓库复杂度上升
2. 不适合所有小改动，轻微修改没必要走完整 task 流程
3. spec 质量决定效果，规范差时收益有限
4. 不同平台对 hooks、skills、sub-agent 的支持不完全一致
5. 它偏流程治理，不是完全自治的编码代理

## 适合怎样落地

### 个人项目
- 先初始化单一平台，如 Claude Code
- 用一个小功能试跑完整流程
- 小改动继续 inline，大改动再创建 task

### 团队项目
- 先由一位负责人初始化并提交基础结构
- 选一个真实小需求跑完整 Plan -> Execute -> Finish
- review 第一版 spec
- 再逐步把团队现有编码规范迁入 `.trellis/spec/`
- 最后让不同工具用户接入自己的平台目录

## 结论

Trellis 的本质是：把 AI Coding 从“对话驱动的临场发挥”转成“仓库内可版本化、可检查、可复用的工程流程”。

如果团队已经频繁使用 Claude Code、Cursor、Codex、Gemini 等工具处理真实工程任务，Trellis 值得试点；如果只是偶尔做一次性的小修改，原生工具通常更轻量。

